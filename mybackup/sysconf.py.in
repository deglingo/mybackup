#

import types

SYSCONF = types.MappingProxyType({
    'PACKAGE':          '%PACKAGE%',
    'pkgsysconfdir':    '%pkgsysconfdir%',
    'pkglocalstatedir': '%pkglocalstatedir%',
    'pkgvardir':        '%pkgvardir%',
})
